const { Sequelize, Datatypes } = require('sequelize');
const sequelize = new Sequelize('sqlite::memory:');

const pokemonTreinador = sequelize.define (
    'pokemonTreinador',
    {
        
    },
    {
        
    },
);